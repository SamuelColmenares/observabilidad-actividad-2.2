# =============================================================================
# Passengers CI — Build & Test
#
# Triggers on push or PR to main when Passengers source files change.
# CD workflow depends on successful completion of this workflow.
# =============================================================================

name: Passengers CI

on:
  push:
    branches: [main]
    paths:
      - 'Passengers/**'
  pull_request:
    branches: [main]
    paths:
      - 'Passengers/**'

jobs:
  build-and-test:
    name: Build and Test Passengers Service
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout ──────────────────────────────────────────────────────────
      - name: Checkout code
        uses: actions/checkout@v4

      # ── 2. Setup .NET 10 ─────────────────────────────────────────────────────
      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      # ── 3. Restore dependencies ───────────────────────────────────────────────
      - name: Restore dependencies
        run: dotnet restore Passengers/Passengers.csproj

      # ── 4. Build ─────────────────────────────────────────────────────────────
      - name: Build solution
        run: dotnet build Passengers/Passengers.csproj --no-restore -c Release

      # ── 5. Run unit tests ────────────────────────────────────────────────────
      # Discovers any *Tests.csproj projects inside the Passengers/ directory tree.
      - name: Run unit tests
        run: |
          TEST_PROJECTS=$(find Passengers -name '*Tests.csproj' -o -name '*Test.csproj' 2>/dev/null)
          if [ -n "$TEST_PROJECTS" ]; then
            echo "$TEST_PROJECTS" | xargs -I{} dotnet test {} --no-build -c Release --logger "trx;LogFileName=test-results.trx" --results-directory ./test-results
          else
            echo "No test projects found in Passengers/. Skipping test step."
          fi

      # ── 6. Upload test results (optional, visible in Actions UI) ─────────────
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: passengers-test-results
          path: test-results/
          retention-days: 7
